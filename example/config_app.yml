name: name_application
#name: name_applications

path: "/vagrant/apps"
#path: "/home/vagner/Projects/rails/zapp_applications"

#ruby:
  #version: "1.9.3p392"
  #manager: rbenv
  #gemset: name_application

test: rspec

git:
  gitignore: ["*.swo", "*.swp"]
  #repository: 'git@name_repository.com:user/name_application.git'
  #first_commit: "First Commit"

rm: ["doc/", "lib/", "vendor/", "public/index.html"]

database:
  adapter:  postgresql
  user:     postgres
  password: postgres

gems:
  groups:
    all:
      rails:
        version: '3.2.12'
        #path:
        #github:
        #tag:
        #branch:
      haml:

    assets:

    development:

    test:

    development_test:
      rspec-rails:

    production:
